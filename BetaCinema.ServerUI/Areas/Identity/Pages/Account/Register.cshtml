@page "/register"
@model RegisterModel

<div class="login-container">
    <div class="login-wrapper">
        <div class="title">Đăng ký tài khoản</div>
        <div class="form-wrapper">
            <form asp-route-returnUrl="@Model.ReturnUrl" id="account" method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-input-wrapper">
                    <label asp-for="Input.FullName" class="form-label">@UserResources.FullName</label>
                    <input asp-for="Input.FullName" class="form-control form-control-sm" />
                    <span asp-validation-for="Input.FullName" class="text-danger"></span>
                </div>

                <div class="form-input-wrapper">
                    <label asp-for="Input.UserName" class="form-label">@UserResources.UserName</label>
                    <input asp-for="Input.UserName" class="form-control form-control-sm" />
                    <span asp-validation-for="Input.UserName" class="text-danger"></span>
                </div>

                <div class="form-input-wrapper">
                    <label asp-for="Input.Password" class="form-label">@UserResources.Password</label>
                    <input asp-for="Input.Password" class="form-control form-control-sm" />
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>

                <div class="form-input-wrapper">
                    <label asp-for="Input.Email" class="form-label">@UserResources.Email</label>
                    <input asp-for="Input.Email" class="form-control form-control-sm" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>

                <button type="submit" class="btn auth-button mt-3">Đăng ký</button>
            </form>

            @if ((Model.ExternalLogins?.Count ?? 0) != 0)
            {
                <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl"
                      method="post" class="form-horizontal mt-3">
                    @foreach (var provider in Model.ExternalLogins)
                    {
                        <button type="submit" class="btn auth-button external" name="provider" value="@provider.Name"
                                title="Log in using your @provider.DisplayName account">
                            Tiếp tục với @provider.DisplayName
                        </button>
                    }
                </form>
            }
        </div>
    </div>
</div>